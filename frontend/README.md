# Frontend (Next.js)

## Run locally

```bash
cd frontend
npm install
cp .env.example .env.local
npm run dev
```

## URL

- `http://localhost:3000`

## Environment

- `NEXT_PUBLIC_API_URL` points to FastAPI backend, default `http://localhost:8000/api/v1`
